import styled from "styled-components";

const Recommends = (props) => {
    return (
        <div>
            <h4>
                Recommended For you
            </h4>
        </div>
    );
}

export default Recommends;